/** @format */

import { NextPageAuth } from '../../src/providers/privateRoutes.interface'

type Props = {}

const ProfilePage: NextPageAuth = (props: Props) => {
  return <div>index</div>
}

ProfilePage.isOnlyUser = true

export default ProfilePage
